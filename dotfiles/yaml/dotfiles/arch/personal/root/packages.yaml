%YAML 1.2
---
# This dotfiles file lists the packages-related dotfiles to be included on Arch
# only for my personal machine for root.

dotfiles:
- f_pacman_hooks.bin_install-rust-toolchain
- f_reflector.conf
