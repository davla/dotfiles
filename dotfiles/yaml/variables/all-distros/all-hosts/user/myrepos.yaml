%YAML 1.2
---
# This variables file defines variables used by myrepos configuration for
# unprivileged users

variables:
  # These variables are used by Pokémon Central Wiki-related software.
  wiki_path: ~/code/wiki
  wiki_util_path: '{{@@ wiki_path @@}}/util'
  pywikibot_path: '{{@@ wiki_path @@}}/pywikibot'
  wiki_lua_modules_path: '{{@@ wiki_path @@}}/lua-modules'
  pywikibot_venv_path: '{{@@ pywikibot_path @@}}/.venv'
