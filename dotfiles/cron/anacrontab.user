# {{@@ header() @@}}
#
# /etc/anacrontab: configuration file for anacron
#
# See anacron(8) and anacrontab(5) for details.

SHELL=/bin/sh
PATH=/usr/local/bin:/bin:/usr/bin

#1   1   repos.update            mr -md "$HOME" update 2>&1 | grep update | tac | cron-logger 'MYREPOS'
