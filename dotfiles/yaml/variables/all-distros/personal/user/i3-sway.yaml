%YAML 1.2
---
# This variables file defines some i3-related variables for my personal system
# only, for unprivileged users.

variables:
  browser: firefox

  i3blocks_temperature_chip: coretemp-isa-0000

  mail_client: thunderbird

  messaging_apps: [ telegram, signal, caprine ]
  messaging_client: messaging
  messaging_mark: messaging
  messaging_client_cmd: >-
    messaging
    --mark {{@@ messaging_mark @@}}
    --workspace messaging
    {{@@ messaging_apps | join(" ") @@}}
