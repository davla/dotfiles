# This file preserves in sudo the rustup configuration environment variables
#
# {{@@ header() @@}}

Defaults    env_keep += "{{@@ vars_with_prefix('rustup_env_var').keys()
    | map('upper') | join(' ') @@}}"
