actions:
  pre:
    strip_range: >
      sed -i -E '/{0}/,/{1}/ {{
          /{0}/n
          /{1}/ ! {{
            d
          }}
        }}' {2}
  post:
    insert_match: "sed -i -E '/{0}/ r {1}' {2}"
    # insert_at: "sed -i '{0} r {1}' {2}"
    rm: rm -f {0}
