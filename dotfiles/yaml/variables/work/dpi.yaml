%YAML 1.2
---
# This variables file defines variables related to DPI for my work system

variables:
  # These variables define paths that change for system with multi-dpi support
  dunstrc_lodpi_dst: '{{@@ xdg_config("dunst/dunstrc") @@}}'
  gtk2_gtkrc_lodpi_dst: gtkrc
  gtk3_settings_lodpi_dst: '{{@@ xdg_config("gtk-3.0/settings.ini") @@}}'
