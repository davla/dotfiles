# This file defines default values for sudo environment variables
#
# {{@@ header() @@}}

{#@@
    rustup variables can be reused from the environment snippets
-@@#}

{%@@ if env['HOST'] != 'work' -@@%}
    {%@@ include 'etc/rustup-environment' @@%}
{%@@ endif @@%}
