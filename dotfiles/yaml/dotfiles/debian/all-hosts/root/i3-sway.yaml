%YAML 1.2
---
# This dotfiles file lists the dotfiles to be included in the "i3-sway" profile
# only for debian for root.

dotfiles:
- f_80i3blocks
- f_i3blocks_gh-release-updates
- f_i3blocks_nmcli-connection-status
