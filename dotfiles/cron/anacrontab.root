# {{@@ anacrontab_append_marker @@}}
#
# {{@@ header() @@}}

1   1   manual.update           mr -d /opt -c /opt/.mrconfig upgrade 2>&1 | grep -v 'finish' | grep -vP '^\S*$' | cron-logger 'MANUAL-INSTALL'
1   1   host.refresh            host-refresh > /dev/null 2>&1; cron-logger 'HOST-REFRESH' "$?"
