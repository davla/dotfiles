%YAML 1.2
---
# This variables file defines some i3-related variables for my work system
# only, for root

variables:
  # These are the i3blocks contrib blocklets that are installed system-wide
  i3blocks_contrib_list: >-
    apt-upgrades
    batterybar
    cpu_usage2
    temperature
