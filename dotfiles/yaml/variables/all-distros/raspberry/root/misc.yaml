%YAML 1.2
---
# This variables file defines some miscellaneous host-specific variables for my
# raspberry pi, for the root user.

variables:

  # This variable defines the Zygal shell theme colorscheme.
  zygal_colorscheme: red
