%YAML 1.2
---
# This variables file defines some i3-related variables specific for my work
# system only, for unprivileged users.

variables:
  browser: firefox

  messaging:
    cmd: com.slack.Slack
    criterion: instance="slack"
    apps: null
