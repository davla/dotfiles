[user]
	name = Davide Laezza
	email = truzzialrogo@gmx.com
[credential]
	helper = /usr/bin/git-credential-gnome-keyring
[gitreview]
	username = davla
[alias]
	graph = log --all --graph --format='%C(yellow)%h %Creset%s%Cred%d%n'
	info = status -sb
	ls-aliases = config --get-regexp ^alias\\.
	squash = merge --squash
	unstash = stash pop
	unadd = reset HEAD --
	update = pull --rebase

	a = add
	b = checkout -qb
	c = commit
	g = log --all --graph --format='%C(yellow)%h %Creset%s%Cred%d%n'
	i = status -sb
	l = log
    m = merge --ff-only
	p = push
	r = rebase
	s = stash push --
	u = pull --rebase

	aa = add --all
	ck = checkout -q
    cp = cherry-pick
	sq = merge --squash
	ua = reset HEAD --
	us = stash pop
