%YAML 1.2
---
# This variables file defines git-related variables for my raspberry pi

variables:
  git_commit_gpg_sign: true
  git_username: davla
  # Used by `git review` command, mostly for gerrit platforms
  gitreview_username: davla
