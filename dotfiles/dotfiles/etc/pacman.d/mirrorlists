# This pacman configuration files contains additional mirrorlists
#
# {{@@ header() @@}}

{#@@ Chaotic AUR is x86_64-only @@#}
{%@@ if env['HOST'] != 'raspberry' @@%}
[chaotic-aur]
Include = /etc/pacman.d/chaotic-mirrorlist
{%@@ endif @@%}
